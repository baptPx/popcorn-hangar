let mapService = require('../../src/services/MapService')
import {describe, expect, test} from '@jest/globals';

describe('tests map service', () => {
    test('fct add works', () => {
        // expect(mapService.add(1,2)).toBe(3)
    })

})
